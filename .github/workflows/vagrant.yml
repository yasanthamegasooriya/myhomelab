name: Vagrant provision the homelab
on:
    push:
        branches:
            - "master"
            - "main"
jobs:
    deploy:
        runs-on: self-hosted
        steps:
          - name: Checkout
            uses: actions/checkout@v3
          - name: create kind cluster
            run: kind create cluster --config ./kindCluster/kind-example-config.yaml
